var MongoClient = require('mongodb').MongoClient
var vars = require('../vars');

MongoClient.connect(vars.database, function (err, db) {
  if (err) throw err

  db.collection('api-keys').find().toArray(function (err, result) {
    if (err) throw err

    console.log(result)
  })
})
